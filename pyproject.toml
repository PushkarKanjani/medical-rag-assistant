[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "medassist"
version = "0.1.0"
description = "Pushkar MedAssist"
requires-python = ">=3.12"
authors = [{ name = "Pushkar Kanjani" }]

[tool.pytest.ini_options]
asyncio_mode = "auto"